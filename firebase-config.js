// Fill these in with your own Firebase project's web app config
// (Firebase console > Project settings > General > Your apps > Web app > SDK setup).
// This config is safe to commit/publish — it just identifies your project.
// Real security comes from the Firestore rules, not from hiding this file.
//
// Until you fill this in, cloud sync stays disabled and the site keeps
// working exactly as before (localStorage only).
const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
